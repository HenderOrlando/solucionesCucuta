{% extends '::base.html.twig' %}

{% block body -%}
    <div class="uk-width-9-10 uk-container-center">
        <h1>Lista de Estados</h1>
        <div class="uk-overflow-container">
            <table class="uk-table uk-table-hover uk-table-striped uk-table-middle">
                <thead>
                <tr>
                    <th class="uk-text-center">Id</th>
                    <th class="uk-text-center">Nombre</th>
                    <th class="uk-text-center">Slug</th>
                    <th class="uk-text-center">Descripcion</th>
                    <th class="uk-text-center">Acciones</th>
                </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>
                        <td><a href="{{ path('estado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                        <td>{{ entity.nombre }}</td>
                        <td>{{ entity.slug }}</td>
                        <td>{{ entity.descripcion }}</td>
                        <td>
                            <a href="{{ path('estado_show', { 'id': entity.id }) }}" class="uk-button uk-button-large uk-width-1-1 uk-margin-small-bottom">
                                Mostrar
                            </a>
                            <a href="{{ path('estado_edit', { 'id': entity.id }) }}" class="uk-button uk-button-large uk-width-1-1 uk-margin-small-bottom">
                                Editar
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="uk-button-group uk-margin">
            <a href="{{ path('estado_new') }}" class="uk-button uk-button-large">
                Crear Nuevo Estado
            </a>
        </div>
    </div>
{% endblock %}